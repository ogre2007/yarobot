[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "yarobot"
requires-python = ">=3.8"
dependencies = [
    "lief>=0.13.0",
    "lxml>=4.9.0",
    "orjson>=3.11", 
]
version = "0.25"

[tool.maturin]
features = ["pyo3/extension-module"]